// Test basic variable declarations and types
int x = 42;
float pi = 3.14;
string message = "Hello, World!";
boolean flag = true;
char letter = 'A';

int main() {
    // Local variables
    int local = x + 10;
    float result = pi * 2.0;
    
    // Type promotion
    float mixed = x + pi;  // int promoted to float
    
    // String concatenation
    string greeting = message + " from TypeChecker!";
    
    print(greeting);
    return 0;
}