int main() {
    int number = 42;
    string text = "Hello";
    
    // Type error: cannot add string to int
    int bad1 = number + text;
    
    // Type error: cannot assign string to int
    number = text;
    
    // Type error: undefined variable
    int bad2 = undefinedVar + 10;
    
    // Type error: condition must be boolean
    if (number) {
        return 1;
    }
    
    // Type error: array index must be int
    int[] arr = new int[10];
    int value = arr[text];
    
    return 0;
}